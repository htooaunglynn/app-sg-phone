<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - DataHub</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body style="background-color: var(--bg-primary); color: var(--text-primary);" class="font-sans">
    <div class="min-h-screen flex items-center justify-center px-4">
        <div class="w-full max-w-sm">
            <!-- Logo -->
            <div class="text-center mb-8">
                <div class="flex justify-center mb-4">
                    <div class="w-12 h-12 rounded-lg flex items-center justify-center" style="background-color: var(--accent-blue);">
                        <span class="font-bold text-lg" style="color: var(--text-primary);">D</span>
                    </div>
                </div>
                <h1 class="text-3xl font-semibold mb-2" style="color: var(--text-primary);">DataHub</h1>
                <p style="color: var(--text-tertiary);">Sign in to your account</p>
            </div>

            <!-- Form -->
            <form id="loginForm" class="space-y-4" method="POST" action="/auth/login">
                <div>
                    <label class="block text-sm font-medium mb-2" style="color: var(--text-tertiary);">Email</label>
                    <input type="email" id="loginEmail" name="email" required
                        class="modal-input w-full"
                        placeholder="you@example.com">
                </div>

                <div>
                    <label class="block text-sm font-medium mb-2" style="color: var(--text-tertiary);">Password</label>
                    <input type="password" id="loginPassword" name="password" required
                        class="modal-input w-full"
                        placeholder="••••••••">
                </div>

                <% if (typeof error !=='undefined' && error) { %>
                    <div id="loginError" class="px-4 py-2 rounded-lg text-sm" style="background-color: var(--validation-invalid); color: var(--accent-red); border: 1px solid var(--accent-red);">
                        <%= error %>
                    </div>
                    <% } %>

                        <button type="submit"
                            class="btn-primary w-full">
                            Sign In
                        </button>
            </form>

            <!-- Register Link -->
            <div class="text-center mt-6">
                <p style="color: var(--text-tertiary);">
                    Don't have an account?
                    <a href="/register" class="font-medium hover:underline" style="color: var(--accent-blue);">Sign up</a>
                </p>
            </div>
        </div>
    </div>

</body>

</html>
